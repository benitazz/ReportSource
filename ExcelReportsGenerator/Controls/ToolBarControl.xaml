<UserControl x:Class="ExcelReportsGenerator.Controls.ToolBarControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="30" 
             d:DesignWidth="800">
    <Border BorderThickness="0,0,0,1"
            BorderBrush="DarkGray">
        <ToolBarTray x:Name="Toolbar">
            <ToolBar>
                <Button ToolTip="Open Report"
                        Command="{Binding OpenCommand}">
                    <Image Source="../Resources/Images/folder_page.png"  />
                </Button>
            </ToolBar>
            <ToolBar>
                <Button ToolTip="Generate Report"
                        Command="{Binding ReportGenerateCommand}">
                    <Image Source="../Resources/Images/report.png"  />
                </Button>
            </ToolBar>
        </ToolBarTray>
    </Border>
</UserControl>
